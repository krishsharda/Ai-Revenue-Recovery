# The Next dev server proxies /api to this backend origin, so the whole app is
# reachable on one URL (http://localhost:3000) in local development.
# Not needed on Vercel: vercel.json routes /api to the API service.
BACKEND_ORIGIN=http://127.0.0.1:8000
